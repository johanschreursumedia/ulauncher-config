{
  "launcherType": "bin",
  "config": {
    "executable": "$( locres apps/deadline/$UVER_DEADLINECOMMAND_VERSION )/bin/$USYS_OS/bin/deadlineslave"
  },
  "env": {
    "override" : {
      "DEADLINE_PATH": "$( locres apps/deadline/$UVER_DEADLINECOMMAND_VERSION )/bin/$USYS_OS/bin"
    }
  }
}
